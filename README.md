# 🛡️ CyberShield AI - Phishing Detector

Интеллектуальная система анализа веб-ресурсов для выявления фишинговых сайтов на основе машинного обучения и эвристического анализа.

## 📋 Содержание

- [Описание](#описание)
- [Архитектура системы](#архитектура-системы)
- [Структура кода](#структура-кода)
- [Как работает определение фишинга](#как-работает-определение-фишинга)
- [15 признаков фишинга (детально)](#15-признаков-фишинга-детально)
- [Установка и запуск](#установка-и-запуск)
- [Обучение модели](#обучение-модели)
- [Использование](#использование)

## 📖 Описание

CyberShield AI - это веб-приложение на базе Streamlit, которое анализирует URL и определяет вероятность того, что сайт является фишинговым. Система использует комбинацию:

- **Машинное обучение** (Random Forest Classifier)
- **Эвристический анализ** (15 признаков)
- **Whitelist известных доменов**
- **Проверка доступности сайта**

Модель обучена на датасете из **~1.4 миллиона URL** из 5 различных источников.

## 🏗️ Архитектура системы

### Компоненты:

1. **`app.py`** - Основное веб-приложение (Streamlit)
   - Интерфейс пользователя
   - Загрузка и использование обученной модели
   - Проверка доступности сайтов
   - Визуализация результатов

2. **`train_model.py`** - Скрипт обучения модели
   - Загрузка данных из CSV файлов
   - Извлечение признаков
   - Обучение Random Forest
   - Сохранение модели

### Процесс работы:

```
URL → Проверка доступности → Извлечение признаков → ML классификация → Результат
```

## 💻 Структура кода

### `app.py` - Основное приложение

**Импорты и настройка:**
- Библиотеки: `streamlit`, `numpy`, `pandas`, `sklearn`, `requests`, `joblib`
- Поддержка Punycode через `idna` (для кириллических доменов)
- Настройка страницы Streamlit и CSS стилей

**Основные функции:**

1. **`calculate_entropy(text)`**
   - Вычисляет энтропию Шеннона для строки
   - Используется для определения случайности домена
   - Возвращает значение от 0 до ~8 бит

2. **`is_known_legitimate_domain(hostname)`**
   - Проверяет, является ли домен известным легитимным
   - Используется только для мягкой проверки доступности (UX)
   - Возвращает True/False

3. **`check_site_availability(url, timeout, is_known_domain)`**
   - Проверяет доступность сайта через HTTP/HTTPS запрос
   - Обрабатывает различные коды ответа (404, 403, 5xx и т.д.)
   - Для известных доменов проверка более мягкая
   - Возвращает: `(is_available, error_message, status_code)`

4. **`normalize_hostname(hostname)`**
   - Конвертирует Punycode домены в Unicode (xn--p1ai → .рф)
   - Обрабатывает кириллические домены
   - Возвращает нормализованный домен

5. **`extract_features(url)`**
   - Главная функция извлечения признаков
   - Парсит URL, нормализует домен
   - Извлекает 15 признаков из URL
   - Возвращает вектор из 15 чисел

6. **`load_and_train_model(use_csv)`**
   - Загружает и обучает модель (fallback, если нет сохраненной модели)
   - Используется только для демонстрации
   - Возвращает: `(model, accuracy, dataset_size)`

7. **`load_model()`**
   - Загружает сохраненную модель из `phishing_model.pkl`
   - Если модель не найдена, использует демонстрационный датасет
   - Использует `@st.cache_resource` для кэширования
   - Возвращает: `(model, accuracy, dataset_size)`

**Основной поток выполнения:**

```python
# 1. Пользователь вводит URL
url_input = st.text_input(...)

# 2. Проверка доступности
is_available = check_site_availability(url)

# 3. Извлечение признаков
features = extract_features(url)

# 4. Классификация ML моделью
probability = model.predict_proba(features)[0][1]

# 5. Отображение результата
if probability > 0.7: # Фишинг
elif probability > 0.4: # Подозрительно
else: # Безопасно
```

### `train_model.py` - Обучение модели

**Структура:**

1. **Импорты и настройка**
   - Те же библиотеки, что и в `app.py`
   - Настройка UTF-8 для Windows

2. **Функции извлечения признаков**
   - `calculate_entropy()` - идентична версии в `app.py`
   - `normalize_hostname()` - идентична версии в `app.py`
   - `extract_features()` - идентична версии в `app.py`
   - **Важно:** Функции должны быть идентичны в обоих файлах!

3. **Загрузка данных (5 этапов)**
   ```python
   # Этап 1: Загрузка CSV файлов
   - legitimate_dataset.csv → legit_data
   - malicious_phish.csv → legit_data (benign) + phishing_data
   - phishing_site_urls.csv → legit_data (good) + phishing_data (bad)
   - PhiUSIIL_Phishing_URL_Dataset.csv → legit_data (label=0) + phishing_data (label=1)
   - dataset_example.csv → legit_data + phishing_data
   
   # Этап 2: Удаление дубликатов
   df_legit.drop_duplicates()
   df_phishing.drop_duplicates()
   
   # Этап 3: Извлечение признаков
   for url in df:
       features = extract_features(url)
       data.append(features)
       labels.append(label)
   
   # Этап 4: Разделение на train/test (80/20)
   X_train, X_test, y_train, y_test = train_test_split(...)
   
   # Этап 5: Обучение модели
   clf = RandomForestClassifier(...)
   clf.fit(X_train, y_train)
   ```

4. **Оценка и сохранение**
   - Вычисление точности на train и test выборках
   - Вывод classification_report и confusion_matrix
   - Сохранение модели в `phishing_model.pkl`

**Параметры обучения:**
- `n_estimators=200` - количество деревьев
- `max_depth=20` - максимальная глубина дерева
- `min_samples_split=5` - минимум образцов для разделения
- `min_samples_leaf=2` - минимум образцов в листе
- `n_jobs=-1` - использование всех ядер CPU
- `random_state=42` - для воспроизводимости

### Ключевые моменты

**Синхронизация функций:**
- Функции `extract_features()`, `calculate_entropy()`, `normalize_hostname()` должны быть **идентичны** в `app.py` и `train_model.py`
- Если изменить извлечение признаков в одном файле, нужно изменить и в другом!

**Поток данных:**
```
CSV файлы → Загрузка → Извлечение признаков → Обучение → Сохранение модели
                                                              ↓
URL → Извлечение признаков → Загрузка модели → Классификация → Результат
```

**Кэширование:**
- `@st.cache_resource` используется для кэширования модели
- Модель загружается один раз при первом запуске
- При изменении `phishing_model.pkl` нужно перезапустить приложение

## 🔍 Как работает определение фишинга

### Этап 1: Проверка доступности сайта

Система сначала проверяет, существует ли сайт:
- Проверка DNS (разрешение доменного имени)
- HTTP/HTTPS запрос с таймаутом
- Анализ кодов ответа сервера

**Важно:** Если сервер отвечает (даже с кодом 404, 403 и т.д.), сайт считается существующим. Только реальные ошибки DNS или отсутствие соединения означают, что сайт недоступен.

### Этап 2: Извлечение признаков

Из URL извлекаются **15 признаков**, которые описывают характеристики сайта:

1. UsingIP
2. LongURL
3. ShortURL
4. Symbol@
5. Redirecting//
6. PrefixSuffix-
7. SubDomains
8. HTTPS
9. DomainRegLen
10. NonStdPort
11. AbnormalURL
12. Entropy
13. SuspiciousWords
14. KnownDomain
15. Hyphens

### Этап 3: Классификация ML моделью

Обученная модель Random Forest анализирует вектор из 15 признаков и возвращает вероятность фишинга (0-1).

### Этап 4: Интерпретация результата

- **> 70%** - 🚨 Фишинговая атака обнаружена
- **40-70%** - ⚠️ Подозрительная активность
- **< 40%** - ✅ Ресурс безопасен

## 📊 15 признаков фишинга (детально)

### 1. **UsingIP** (Использование IP-адреса)
- **Что проверяет:** Наличие IP-адреса вместо доменного имени
- **Пример:** `http://192.168.1.1/login` вместо `http://example.com/login`
- **Почему подозрительно:** Легитимные сайты используют доменные имена, а не IP-адреса
- **Значение:** 1 = подозрительно, 0 = нормально

### 2. **LongURL** (Длинный URL)
- **Что проверяет:** Длина URL больше 75 символов
- **Пример:** `https://example.com/very/long/path/with/many/segments/and/parameters?param1=value1&param2=value2...`
- **Почему подозрительно:** Фишинговые сайты часто используют длинные URL для сокрытия реального адреса
- **Значение:** 1 = подозрительно (>75 символов), 0 = нормально

### 3. **ShortURL** (Сокращенный URL)
- **Что проверяет:** Использование сервисов сокращения ссылок
- **Сервисы:** bit.ly, tinyurl.com, goo.gl, t.co, ow.ly, is.gd
- **Почему подозрительно:** Сокращенные ссылки скрывают реальный адрес, что часто используется в фишинге
- **Значение:** 1 = подозрительно, 0 = нормально

### 4. **Symbol@** (Символ @ в URL)
- **Что проверяет:** Наличие символа @ в URL
- **Пример:** `http://user@example.com` или `http://example.com@malicious.com`
- **Почему подозрительно:** Символ @ используется для обмана - часть после @ является реальным доменом
- **Значение:** 1 = подозрительно, 0 = нормально

### 5. **Redirecting//** (Двойной слеш)
- **Что проверяет:** Наличие двойного слеша `//` после протокола
- **Пример:** `https://example.com//malicious-site`
- **Почему подозрительно:** Может использоваться для обхода фильтров или редиректов
- **Значение:** 1 = подозрительно, 0 = нормально

### 6. **PrefixSuffix-** (Дефис в начале/конце домена)
- **Что проверяет:** Дефис в начале или конце домена или его частей
- **Пример:** `-example.com`, `example-.com`, `sub--domain.com`
- **Почему подозрительно:** Легитимные домены редко имеют дефисы в начале/конце (хотя дефисы в середине допустимы)
- **Значение:** 1 = подозрительно, 0 = нормально

### 7. **SubDomains** (Количество поддоменов)
- **Что проверяет:** Количество точек в домене (количество поддоменов)
- **Пример:** 
  - `example.com` → 0 поддоменов
  - `www.example.com` → 1 поддомен
  - `sub1.sub2.example.com` → 2 поддомена
- **Почему подозрительно:** Много поддоменов может указывать на попытку имитации легитимного домена
- **Значение:** Число (0, 1, 2, 3...)

### 8. **HTTPS** (Наличие HTTPS)
- **Что проверяет:** Использование протокола HTTPS
- **Почему важно:** HTTPS обеспечивает шифрование, его отсутствие подозрительно
- **Значение:** 1 = HTTPS есть, 0 = только HTTP

### 9. **DomainRegLen** (Длина домена без TLD)
- **Что проверяет:** Длина основной части домена (без .com, .ru и т.д.)
- **Пример:** 
  - `example.com` → длина = 7
  - `very-long-domain-name.com` → длина = 22
- **Почему важно:** Очень короткие или очень длинные домены могут быть подозрительными
- **Особенность:** Для известных коротких доменов (ya.ru, go.com) используется нормализация

### 10. **NonStdPort** (Нестандартный порт)
- **Что проверяет:** Использование портов, отличных от стандартных (80, 443, 8080)
- **Пример:** `example.com:8081`, `example.com:3000`
- **Почему подозрительно:** Легитимные сайты обычно используют стандартные порты
- **Значение:** 1 = нестандартный порт, 0 = стандартный

### 11. **AbnormalURL** (Аномальный URL)
- **Что проверяет:** Количество специальных символов в URL
- **Символы:** `%`, `&`, `=`, `?`, `#`, `+`
- **Порог:** Более 3 вхождений считается подозрительным
- **Пример:** `example.com?param1=value1&param2=value2&param3=value3#section`
- **Почему подозрительно:** Много параметров может указывать на попытку обфускации
- **Значение:** 1 = подозрительно (>3 символов), 0 = нормально

### 12. **Entropy** (Энтропия домена)
- **Что проверяет:** Энтропия Шеннона для домена (мера случайности/сложности)
- **Формула:** H(X) = -Σ p(x) * log₂(p(x))
- **Высокая энтропия:** Случайные последовательности символов (подозрительно)
- **Низкая энтропия:** Осмысленные слова (нормально)
- **Пример:** 
  - `google.com` → низкая энтропия (осмысленное слово)
  - `x7k9m2p.com` → высокая энтропия (случайные символы)
- **Значение:** Число от 0 до ~8 (бит)

### 13. **SuspiciousWords** (Подозрительные слова)
- **Что проверяет:** Наличие подозрительных слов в URL
- **Слова везде:** `verify`, `update`, `secure`, `confirm`, `validate`
- **Слова в пути:** `login`, `account`, `bank` (только если домен не известный)
- **Почему подозрительно:** Фишинговые сайты часто используют эти слова для создания ощущения срочности
- **Особенность:** Для известных доменов слова в пути не считаются подозрительными
- **Значение:** Количество найденных слов (0, 1, 2, 3...)

### 14. **KnownDomain** (Известный домен)
- **Что проверяет:** Принадлежность домена к whitelist известных легитимных ресурсов
- **Список включает:** 
  - Международные: google.com, facebook.com, amazon.com и т.д.
  - Российские: vk.com, yandex.ru, sberbank.ru и т.д.
  - Банки, госуслуги, СМИ, магазины и т.д.
- **Почему важно:** Известные домены практически никогда не являются фишинговыми
- **Значение:** 1 = известный домен, 0 = неизвестный

### 15. **Hyphens** (Количество дефисов в домене)
- **Что проверяет:** Общее количество дефисов в доменном имени
- **Пример:** 
  - `example.com` → 0 дефисов
  - `example-site.com` → 1 дефис
  - `very-long-domain-name.com` → 3 дефиса
- **Почему важно:** Много дефисов может указывать на попытку имитации легитимного домена
- **Значение:** Количество дефисов (0, 1, 2, 3...)

## 🚀 Установка и запуск

### Требования

```bash
pip install -r requirements.txt
```

Зависимости:
- streamlit
- pandas
- numpy
- scikit-learn
- requests
- joblib

### Запуск приложения

```bash
python3 -m streamlit run app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

## 🎓 Обучение модели

### Подготовка данных

Поместите CSV файлы в корень проекта:

- `malicious_phish.csv` - формат: `url,type` (type: phishing/benign/defacement/malware)
- `phishing_site_urls.csv` - формат: `URL,Label` (Label: good/bad)
- `PhiUSIIL_Phishing_URL_Dataset.csv` - формат: `URL,...,label` (label: 0/1)
- `legitimate_dataset.csv` - формат: `url,label` (label: 0)
- `dataset_example.csv` - формат: `url,label` (label: 0/1)

### Запуск обучения

```bash
python3 train_model.py
```

Процесс обучения:
1. Загрузка всех CSV файлов (~1.4M записей)
2. Извлечение признаков из каждого URL
3. Разделение на обучающую и тестовую выборки (80/20)
4. Обучение Random Forest (200 деревьев)
5. Оценка качества модели
6. Сохранение модели в `phishing_model.pkl`

### Параметры модели

- **Алгоритм:** Random Forest Classifier
- **Количество деревьев:** 200
- **Максимальная глубина:** 20
- **Минимум образцов для разделения:** 5
- **Минимум образцов в листе:** 2
- **Параллелизм:** Все доступные ядра CPU

## 💻 Использование

1. **Откройте приложение** в браузере
2. **Введите URL** в поле ввода (можно с протоколом или без)
3. **Нажмите "ЗАПУСТИТЬ СКАНИРОВАНИЕ"**
4. **Получите результат:**
   - Вероятность фишинга (0-100%)
   - Вектор из 15 признаков
   - Рекомендации по безопасности

### Интерпретация результатов

- **🚨 УГРОЗА ОБНАРУЖЕНА (>70%)** - Высокая вероятность фишинга, не переходите по ссылке
- **⚠️ ТРЕБУЕТ ВНИМАНИЯ (40-70%)** - Подозрительные признаки, рекомендуется дополнительная проверка
- **✅ БЕЗОПАСНО (<40%)** - Признаков фишинга не обнаружено

## 📈 Производительность

- **Размер датасета:** ~1,437,000 URL
- **Точность модели:** ~95% (зависит от датасета)
- **Время анализа:** < 5 секунд на URL
- **Признаков:** 15

## 🔧 Технические детали

### Нормализация доменов

Система поддерживает:
- **Punycode декодирование** (xn--p1ai → .рф)
- **Кириллические домены** (.рф, .москва и т.д.)
- **Поддомены** (www, mail, api и т.д.)

### Проверка доступности

- Таймаут: 5 секунд
- Поддержка HTTP и HTTPS
- Обработка редиректов
- Мягкая проверка для известных доменов

### Whitelist доменов

Включает 200+ известных легитимных доменов:
- Социальные сети (vk.com, facebook.com)
- Поисковики (google.com, yandex.ru)
- Банки (sberbank.ru, tinkoff.ru)
- Госуслуги (gosuslugi.ru)
- И многие другие

## 📝 Лицензия

Проект создан в образовательных целях.

## 🤝 Вклад

Для улучшения системы можно:
- Добавить новые признаки
- Расширить whitelist доменов
- Улучшить алгоритм обучения
- Добавить поддержку новых форматов данных

---

**Версия:** 1.0  
**Последнее обновление:** 2024

